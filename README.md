```mermaid
flowchart TD
    A[毎朝 GASトリガー起動] --> B{① 昨日の定例予定は<br>あったか？}
    
    B -- No（予定なし） --> C[スキップ：実行ログDBに記録して終了]
    B -- Yes（予定あり） --> D[Googleドライブ内を自動検索]
    
    D --> E{② 該当 Docs が<br>存在するか？}
    
    E -- No（未検出） --> F[Slackに「議事録未検出」通知 / 実行ログDB記録]
    E -- Yes（検出） --> G[WS内 AI要約処理を実行]
    
    G --> H{③ AI要約処理は<br>成功したか？}
    
    H -- No（エラー） --> I[管理者へエラー通知 / 実行ログDBにエラー記録]
    H -- Yes（成功） --> J[Slack times チャンネルへ要約配信 ＆ 実行ログDB記録]
    
    J --> K{④ ユーザーが<br>用語解説ボタンを押下？}
    
    K -- No --> L[待機終了]
    K -- Yes --> M[マスターDB（用語集）を参照] 
    M --> N[Slack上に用語解説を動的展開]
    N --> O[実行ログDBに「用語閲覧ログ」を記録]
    O --> P[Slackへ処理完了レスポンス返却 / イベント終了]
